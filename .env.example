# ============================================================
# MY NEIGHBOR IS AN ARTIST — Environment Variables
# Copy to .env.local and fill in. NEVER commit .env.local.
# ============================================================

# --- Supabase (DB + auth + image storage) ----------------------
# https://supabase.com/dashboard/project/ykpkjibzhtpxjoonvnba/settings/api-keys
NEXT_PUBLIC_SUPABASE_URL=https://ykpkjibzhtpxjoonvnba.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key — only for server-side code (Stripe webhook, etc).
# Treat like a password. Never put in client code.
SUPABASE_SERVICE_ROLE_KEY=

# --- Stripe (payments + Connect) -------------------------------
# https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
# Webhook signing secret from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_...
